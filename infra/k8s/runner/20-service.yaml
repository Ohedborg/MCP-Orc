apiVersion: v1
kind: Service
metadata:
  name: mcp-runner
  namespace: mcp-system
spec:
  selector:
    app: mcp-runner
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
